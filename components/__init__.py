"""Dashboard UI components: dedicated DQ and monitoring bundles.

Each module exposes HTML helpers or a string constant that the renderer
assembles into the final HTML. Mirrors STATpy's components/ folder.
"""
