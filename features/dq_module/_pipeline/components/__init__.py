"""Shared UI components: layout shell, CSS, JS helpers, comparison-mode bar.

Each module exposes a string constant (CSS or JS) that the renderer assembles
into the final HTML. Mirrors STATpy's components/ folder.
"""
