"""Per-dashboard pages.

Each module exposes a `JS` string constant containing the `renderXxx()` JS
function for that tab. The renderer concatenates these into the final HTML.
Mirrors STATpy's `pages/` directory pattern.
"""
