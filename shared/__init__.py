"""Cross-dashboard shared layer: registry types and app-shell theme constants.

Code here is genuinely shared by the app shell and by more than one dashboard
(per the blueprint's "rule of two"). Dashboard-specific logic must not live
here.
"""
